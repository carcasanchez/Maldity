/*#include "World.h"
#include "Player.h"


void Player::Go(const String& dest)
{

		for (int i = 0; i < 52; i++)
		{
			
			if (world->exit[i].orientation.C_str() == dest.C_str())
			{
				for (int j = 0; j < 13;j++)
				if (world->room[j].name.C_str() == (world->exit[i].destination.C_str()))
					position=j;
			}
		}

}*/